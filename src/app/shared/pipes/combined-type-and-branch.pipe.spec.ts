import { CombinedTypeAndBranchPipe } from './combined-type-and-branch.pipe';

describe('CombinedTypeAndBranchPipe', () => {
  it('create an instance', () => {
    const pipe = new CombinedTypeAndBranchPipe();
    expect(pipe).toBeTruthy();
  });
});
