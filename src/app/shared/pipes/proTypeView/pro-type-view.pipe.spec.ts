import { ProTypeViewPipe } from "./pro-type-view.pipe";

describe('ProTypeViewPipe', () => {
  it('create an instance', () => {
    const pipe = new ProTypeViewPipe();
    expect(pipe).toBeTruthy();
  });
});
